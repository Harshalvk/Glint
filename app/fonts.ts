import { Domine, Geist, Inter, Lora, Source_Sans_3 } from "next/font/google";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});
const lora = Lora({
  subsets: ["latin"],
  display: "swap",
});
const domine = Domine({
  subsets: ["latin"],
  display: "swap",
});
const geist = Geist({
  subsets: ["latin"],
  display: "swap",
});

export { inter, lora, domine, geist };
